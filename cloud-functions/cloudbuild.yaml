steps:
  - name: gcr.io/cloud-builders/gcloud
    entrypoint: bash
    args: ["cloud-functions/build.sh"]
    env:
      - "REPO_NAME=$REPO_NAME"
      - "BRANCH_NAME=$BRANCH_NAME"
      - "COMMIT_SHA=$COMMIT_SHA"
      - "COMMIT_STATE_BUCKET=wemade-cloud-build"
